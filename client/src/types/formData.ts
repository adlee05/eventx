type formData = {
  title: string,
  desc: string,
  location: string,
  maxnums: number,
  imgurl: string,
  category: string,
  startTime: Date,
  startDate: Date,
  deadTime: Date,
  deadDate: Date
};

export type { formData };
