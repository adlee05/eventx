type formData = {
  title: string,
  description: string,
  location: string,
  maxParticipants: number,
  imageUrl: string,
  category: string,
  startTime: string,
  startDate: Date,
  deadTime: string,
  deadDate: Date
};

export type { formData };
