type formData = {
  title: string,
  desc: string,
  location: string,
  maxnums: number,
  imgurl: string,
  category: string,
  startTime: string,
  startDate: Date,
  deadTime: string,
  deadDate: Date
};

export type { formData };
