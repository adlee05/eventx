type EventProps = {
  category: string,
  _id: string,
  startDate: Date,
  startTime: string,
  duration: string,
  title: string,
  description: string,
  location: string,
  imageUrl: string,
  deadTime: string,
  deadDate: Date,
  createdBy: string,
  maxParticipants: string
};

export type { EventProps };
